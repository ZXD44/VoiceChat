# VoiceChat System (Minecraft Bedrock Edition)

**ระบบเสียงตามระยะ (Proximity Voice Chat)** พัฒนาขึ้นเพื่อให้การเล่น Minecraft Bedrock ของคุณสมจริงยิ่งขึ้น!
ระบบนี้ทำงานโดยการเชื่อมต่อระหว่าง **Minecraft Addon** และ **Discord Bot** เพื่อย้ายผู้เล่นใน Discord ตามระยะห่างในเกม

---

## 💎 เครดิต (Credit)
**ระบบนี้พัฒนาโดย: ZirconX**
**Developed by: ZirconX**

> [!IMPORTANT]
> กรุณาให้เครดิต **ZirconX** ทุกครั้งที่มีการนำไปใช้งาน แจกจ่ายต่อ หรือทำคลิปรีวิว
> Please provide credit to **ZirconX** whenever this system is used, redistributed, or featured in a video.

---

## 🚀 ส่วนประกอบ (Components)
โปรเจกต์นี้ประกอบด้วย 2 ส่วนหลักที่ต้องทำงานร่วมกัน:
1. **Minecraft Add-on:** (`scripts/`, `items/`) ติดตั้งในตัวเกม Minecraft หรือ Server
2. **Discord Bot Backend:** (`DiscordBot/`) รันบนเครื่อง Server หรือคอมพิวเตอร์เพื่อจัดการห้องเสียง

---

## 🛠 วิธีการติดตั้ง (Installation Guide)

### ส่วนที่ 1: ติดตั้ง Discord Bot (Backend)
คุณจำเป็นต้องรันบอทตัวนี้เพื่อให้ระบบทำงานได้

1. เข้าไปที่โฟลเดอร์ `DiscordBot`
2. ติดตั้ง Node.js (หากยังไม่มี)
3. เปิด Terminal/Command Prompt ในโฟลเดอร์ `DiscordBot` และพิมพ์คำสั่ง:
   ```bash
   npm install
   ```
4. **ตั้งค่า Config:**
   - เปลี่ยนชื่อไฟล์ `.env.example` เป็น `.env`
   - เปิดไฟล์ `.env` และกรอกข้อมูลให้ครบถ้วน:
     ```env
     DISCORD_TOKEN=Token_บอท_Discord_ของคุณ
     GUILD_ID=ID_เซิร์ฟเวอร์_Discord
     LOBBY_CHANNEL_ID=ID_ห้องเสียง_Lobby_ที่ให้คนไปรอ
     PORT=3000
     API_SECRET=ตั้งรหัสผ่านอะไรก็ได้ (ต้องตรงกับใน scripts/main.js)
     ```
5. **รันบอท:**
   ```bash
   npm start
   ```

### ส่วนที่ 2: ติดตั้ง Minecraft Add-on
1. เปิดไฟล์ `scripts/main.js` และแก้ไขบรรทัด:
   ```javascript
   const API_SECRET = "รหัสผ่านที่ตั้งใน_DiscordBot";
   ```
2. นำโฟลเดอร์ `VoiceChat` ไปใส่ใน `development_behavior_packs` หรือ Zip เป็น `.mcpack` แล้วติดตั้งเข้าเกม
2. เปิดใช้งาน Behavior Pack ในโลกของคุณ
3. **[สำคัญ]** เปิดการตั้งค่า **Experiments** ในหน้าตั้งค่าโลก:
   - [x] **Beta APIs**
4. หากรันบน **Dedicated Server (BDS)** อย่าลืมเพิ่มการอนุญาตใน `permissions.json`:
   ```json
   {
      "allowed_modules": [
         "@minecraft/server",
         "@minecraft/server-ui",
         "@minecraft/server-net"
      ]
   }
   ```

---

## 🎮 วิธีการใช้งาน (Usage)
1. **ใน Discord:** ให้ผู้เล่นทุกคนเข้ามารอในห้องเสียง **Lobby** ที่กำหนดไว้
2. **ใน Minecraft:**
   - ผู้เล่นเข้าเกม
   - พิมพ์คำสั่ง `/give @s voicechat:discord` เพื่อรับไอเทมเมนู
   - ถือไอเทมและ **คลิกขวา** เพื่อเปิดเมนู
   - ตรวจสอบสถานะการเชื่อมต่อ (ชื่อในเกมและชื่อ Discord ต้องตรงกัน หรือระบบสามารถระบุตัวตนได้)
3. **เริ่มเล่น!** เมื่อผู้เล่นเดินเข้าใกล้กันในเกม ระบบจะลากผู้เล่นคนนั้นไปสร้างห้องเสียงส่วนตัวหรือดึงเข้ากลุ่มคุยกันโดยอัตโนมัติ

---

## ⚠️ การแก้ไขปัญหา (Troubleshooting)
- **บอทไม่ทำงาน:** ตรวจสอบไฟล์ `.env` ว่าใส่ Token ถูกต้องหรือไม่
- **เชื่อมต่อไม่ได้:** ตรวจสอบว่าเปิดพอร์ต `3000` (หรือพอร์ตที่ตั้งไว้) แล้วหรือยัง และตรวจสอบ Firewall
- **ชื่อไม่ตรง:** ตรวจสอบว่าชื่อในเกม Minecraft ตรงกับ Nickname หรือ Username ใน Discord

---

## 📜 ข้อตกลงการใช้งาน (License)
ลิขสิทธิ์โดย **ZirconX** ภายใต้ MIT License สามารถนำไปดัดแปลงได้ แต่ต้องคงชื่อผู้พัฒนาเดิมไว้

---
© 2026 ZirconX. แฟนเพจ/ช่องทางติดตาม: [www.zirconx.my](https://www.zirconx.my)

