# 🎙️ Zircon Voice Chat - Proximity System (Backup)

ระบบเสียงตามระยะ (Proximity Voice Chat) สำหรับ Minecraft Bedrock Edition (MCPE/MCBE) พัฒนาโดย **ZirconX** ระบบนี้จะเชื่อมต่อโลก Minecraft ของคุณเข้ากับ Discord โดยอัตโนมัติ เพื่อสร้างประสบการณ์การเล่นที่สมจริงยิ่งขึ้น

---

## 🛠️ ส่วนประกอบของระบบ
ระบบถูกแบ่งออกเป็น 3 ส่วนหลัก:
1.  **DiscordBot**: บอทที่เขียนด้วย Node.js ทำหน้าที่จัดการห้องเสียง (Dynamic Channels)
2.  **VoiceChat BP (Behavior Pack)**: สคริปต์ในเกมที่คำนวณระยะห่างระหว่างผู้เล่น และส่งข้อมูลไปยังบอท
3.  **VoiceChat RP (Resource Pack)**: ทรัพยากรด้านกราฟิกและไอเทมภายในเกม

---

## ✨ ฟีเจอร์เด่น
- **Dynamic Grouping**: เมื่อผู้เล่นเดินมาใกล้กัน ระบบจะดึงเข้าห้องเสียงเดียวกันใน Discord โดยอัตโนมัติ
- **Smart Nametags**: สีชื่อผู้เล่นจะเปลี่ยนไปตามสถานะ (🟢 เขียว = กำลังพูด, 🔴 แดง = ปิดไมค์, ⚪ ขาว = ปกติ)
- **Visual Radius**: ถือไอเทม "Discord" เพื่อดูวงกลมระยะการได้ยินของตัวเอง
- **In-game Settings**: ปรับแต่งระยะ (Radius), เลือกเอฟเฟกต์วงกลม (Particles), หรือเปิด-ปิดระบบได้ในเกม
- **Auto Cleanup**: ลบห้องว่างใน Discord อัตโนมัติเมื่อไม่มีคนอยู่

---

## 🚀 วิธีการติดตั้ง

### 1. การตั้งค่า Discord Bot
1. เข้าไปที่โฟลเดอร์ `DiscordBot`
2. แก้ไขไฟล์ `.env` เพื่อใส่ข้อมูลของคุณ:
   ```env
   DISCORD_TOKEN=โทเค็นของบอท
   GUILD_ID=ไอดีเซิร์ฟเวอร์ดิสคอร์ด
   LOBBY_CHANNEL_ID=ไอดีห้องเสียงหลัก (Lobby)
   PORT=3000
   API_SECRET=mcpe-voice-secret
   ```
3. เปิด Command Prompt / Terminal แล้วติดตั้ง Dependencies:
   ```bash
   npm install
   ```
4. รันบอท:
   ```bash
   node index.js
   ```

### 2. การติดตั้งใน Minecraft
1. นำโฟลเดอร์ `VoiceChat BP` ไปใส่ใน `development_behavior_packs`
2. นำโฟลเดอร์ `VoiceChat RP` ไปใส่ใน `development_resource_packs`
3. ในการสร้างโลก (World Settings) ต้องเปิด **Beta APIs** และ **Server Authoritative Movement** (แนะนำ)
4. สำหรับ Server (เช่น BDS) ต้องแก้ไฟล์ `permissions.json` ในโฟลเดอร์เซิร์ฟเวอร์เพื่ออนุญาตการใช้ Network:
   ```json
   [
     {
       "allowed_modules": [
         "@minecraft/server",
         "@minecraft/server-ui",
         "@minecraft/server-net",
         "@minecraft/server-admin"
       ]
     }
   ]
   ```

---

## 🎮 วิธีใช้งานในเกม
- **เปิดเมนูตั้งค่า**: 
  - ใช้คำสั่ง `/give @s voicechat:discord` เพื่อรับไอเทม แล้วคลิกขวาใช้งาน
  - หรือใช้คำสั่ง `/scriptevent voice:ui`
- **การเชื่อมต่อ**: ชื่อใน Minecraft ของคุณ **ต้องตรงกับ** ชื่อเล่น (Nickname) หรือชื่อผู้ใช้ (Username) ใน Discord เพื่อให้บอทค้นหาตัวตนเจอ
- **ระยะการทำงาน**: ผู้เล่นจะเชื่อมต่อกันเมื่ออยู่ภายในระยะที่ตั้งไว้ (ค่าเริ่มต้นคือ 5-10 บล็อก)

---

## ⚠️ ข้อควรระวัง
- บอทต้องรันอยู่ที่ `localhost:3000` (หรือ IP ที่ระบุใน `main.js` ของ BP)
- หากรันบน VPS ต้องเปิด Port 3000 และแก้ `BACKEND_URL` ใน `VoiceChat BP/scripts/main.js` ให้ตรงกับ IP ของ VPS

---
**สร้างโดย ZirconX**
*Back up created on 2026-01-25*
