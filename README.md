# 🎙️ Zircon Voice Chat - Proximity System

### ระบบเสียงตามระยะ (Proximity Voice Chat) สำหรับ Minecraft Bedrock
**พัฒนาโดย:** ZirconX

[![Version](https://img.shields.io/github/v/release/ZXD44/VoiceChat?label=Version&color=blue)](https://github.com/ZXD44/VoiceChat/releases/latest) [![Download](https://img.shields.io/badge/Download-Latest_Release-2ea44f)](https://github.com/ZXD44/VoiceChat/releases/latest)

---

## มันคืออะไร?
เคยไหม? เล่น Minecraft กับเพื่อนแล้วต้องเปิด Discord คุยกันเสียงดังตลอดเวลา
**ระบบนี้จะช่วยให้:**
- 🗣️ **คุยกันได้เมื่ออยู่ใกล้:** เดินเข้าใกล้เพื่อนจะได้ยินเสียงชัด
- 🤫 **เสียงเบาลงเมื่อเดินห่าง:** ยิ่งไกลเสียงยิ่งเบา จนไม่ได้ยิน
- 🎮 **สมจริงสุดๆ:** สร้างบรรยากาศการเล่น Survival หรือ Roleplay ให้สนุกขึ้น 100%

---

## ⚠️ ข้อกำหนดสำคัญ (Requirements)

> **🖥️ ใช้งานได้เฉพาะ Bedrock Dedicated Server (BDS) เท่านั้น!**

| ✅ รองรับ | ❌ ไม่รองรับ |
|-----------|--------------|
| Bedrock Dedicated Server (BDS) | Single Player |
| เซิร์ฟเวอร์ที่เปิดจากคอม | Realm |
| | เล่นคนเดียว หรือ Host จากมือถือ |

**เหตุผล:** ระบบนี้ใช้ `@minecraft/server-net` ซึ่งเป็น Module ที่ Mojang อนุญาตให้ใช้เฉพาะบน Dedicated Server เท่านั้น เพื่อความปลอดภัย

---

## ✨ มีอะไรเจ๋งๆ บ้าง? (Features)

### 1. 🟢 ระบบห้องอัตโนมัติ (Auto Grouping)
ไม่ต้องกดอะไรทั้งนั้น! แค่เดินเข้าเกม บอทจะดึงคุณไปคุยกับคนที่อยู่ใกล้ๆ เอง เดินหนีก็หลุดจากวงสนทนา สะดวกสุดๆ

### 2. 📛 ป้ายชื่อบอกสถานะ (Smart Nametags)
ดูที่ชื่อบนหัวเพื่อนก็รู้เลยว่าสถานะเป็นไง:
- 🟢 **สีเขียว** = กำลังพูดไฟแลบ!
- 🔴 **สีแดง** = ปิดไมค์อยู่/ไม่ได้ยิน
- ⚪ **สีขาว** = ปกติ/ยังไม่เข้าวงพูดคุย

### 3. 📏 ดูระยะเสียงได้ (Visual Radius)
สงสัยไหมว่าเสียงเราดังไปไกลแค่ไหน?
- ถือไอเทม **"Discord Remote"** 📱
- จะมีวงกลมขึ้นรอบตัวคุณ (ปรับแต่งเอฟเฟกต์ได้ด้วยนะ ใครสายสวยงามต้องชอบ!)

### 4. ⚙️ เมนูตั้งค่าในเกม (In-Game Menu)
กดใช้ไอเทมรีโมท เพื่อเปิดเมนู:
- ปรับระยะการได้ยิน (5 - 10 ช่อง)
- เลือกเอฟเฟกต์วงกลม (หัวใจ, ไฟ, ดาว ฯลฯ)
- เปิด/ปิด ระบบเสียงส่วนตัว

---

## วิธีการติดตั้ง (Step-by-Step)

### ส่วนที่ 1: เตรียมบอท (Discord Bot)
*สำหรับคนเปิดเซิร์ฟเวอร์เท่านั้น (ผู้เล่นไม่ต้องทำ)*

1. เข้าไปที่โฟลเดอร์ `DiscordBot`
2. แก้ไฟล์ชื่อ `.env` (คลิกขวา Open with Notepad) ใส่ข้อมูลให้ครบ:
   ```env
   # 🔑 โทเคนบอท (จาก Discord Developer Portal)
   DISCORD_TOKEN=...รหัสโทเค็นยาวๆ...

   # 🆔 ไอดีเซิร์ฟเวอร์ดิสคอร์ด (Discord Server ID)
   GUILD_ID=123456789...

   # 🏠 ไอดีห้องล็อบบี้เสียง (คนที่รอย้ายจะมารวมกันที่นี่)
   LOBBY_CHANNEL_ID=987654321...

   # 📂 ไอดีหมวดหมู่ (Category ID) ที่จะให้บอทสร้างห้องเสียงใส่
   CATEGORY_ID=555555555...

   # 🔌 พอร์ตของเซิร์ฟเวอร์ API (ปกติ 3000)
   PORT=3000

   # 🔐 รหัสลับสำหรับเชื่อมต่อระหว่าง Minecraft กับ Bot
   API_SECRET=mcpe-voice-secret
   ```
3. เปิด Command Prompt ที่โฟลเดอร์นี้ แล้วพิมพ์:
   ```bash
   npm install
   node index.js
   ```
4. ถ้าขึ้นว่า `Bot Online!` แปลว่าพร้อมลุย!

---

### ส่วนที่ 2: ลงใน Minecraft (ฉบับเข้าใจง่าย)

**ขั้นตอน A: ลงไฟล์เสริม (Addons)**
1. **แตกไฟล์** (Unzip) ที่โหลดมา จะเจอ 2 โฟลเดอร์:
   - 📦 `VoiceChat BP` (ระบบ)
   - 🎨 `VoiceChat RP` (รูปภาพ)
2. **ก๊อปปี้ไปวาง**:
   - กดปุ่ม `Windows + R` พิมพ์ `%localappdata%` กด Enter
   - เข้าไปที่: `Packages` > `Microsoft.MinecraftUWP...` > `LocalState` > `games` > `com.mojang`
   - วาง `VoiceChat BP` ลงใน `development_behavior_packs`
   - วาง `VoiceChat RP` ลงใน `development_resource_packs`

**ขั้นตอน B: ตั้งค่าก่อนเข้าเกม**
ตอนสร้างโลกใหม่ หรือกดรูปดินสอ ✏️ แก้ไขโลกเดิม:
1. **เปิดใช้งาน Packs**:
   - ไปที่ Resource Packs > กด Activate `VoiceChat RP`
   - ไปที่ Behavior Packs > กด Activate `VoiceChat BP`
2. **⚠️ สำคัญมาก! ต้องเปิด** (เมนู Experiments):
   - เลื่อนลงมาล่างสุด เปิด **[Beta APIs]** ไม่งั้นระบบไม่ทำงาน!
3. **แนะนำให้เปิด**:
   - **[Server Authoritative Movement]** (เดินไม่กระตุก ตำแหน่งเป๊ะ)

### 🔧 วิธีตั้งค่า Module (สำหรับเจ้าของเซิร์ฟเวอร์)
*เพื่อให้บอททำงานได้ คุณต้องอนุญาตให้เซิร์ฟเวอร์เชื่อมต่ออินเทอร์เน็ตได้ก่อน:*

1. **หาไฟล์**: เข้าไปที่โฟลเดอร์เซิร์ฟเวอร์ของคุณ มองหาไฟล์ชื่อ `permissions.json` (บางครั้งจะอยู่ในโฟลเดอร์ `config`)
2. **แก้ไข**: คลิกขวา > Open with Notepad
3. **ใส่โค้ด**: เพิ่มรายชื่อ Module ลงไปในรายการ `allowed_modules` ให้ครบตามนี้:

   ```json
   {
      "allowed_modules": [
         "@minecraft/server",
         "@minecraft/server-ui",
         "@minecraft/server-admin",
         "@minecraft/server-gametest", 
         "@minecraft/server-net"      <-- ⚠️ ตัวนี้สำคัญที่สุด! ห้ามลืม
      ]
   }
   ```
4. **บันทึกไฟล์** แล้วเริ่มเซิร์ฟเวอร์ใหม่ได้เลย!

---

## 🎮 วิธีใช้งานจริง

1. **เข้า Discord**: เข้าห้องเสียง "Lobby" หรือห้องที่คุณตั้งค่าไว้
2. **เข้าเกม Minecraft**: ตัวละครของคุณจะถูกเชื่อมต่อทันที
3. **ตรวจสอบชื่อ**:
   - ชื่อในเกม ต้องตรงกับ ชื่อเล่น หรือ ชื่อผู้ใช้ ใน Discord

### 🔧 สำหรับแอดมิน (Admin Config)
*อยากเปลี่ยน IP บอท โดยไม่ต้องแก้ไฟล์? (กรณีรันบอทคนละเครื่องกับเกม)*
1. พิมพ์คำสั่งในเกม: `/tag @s add admin`
2. เปิดเมนูตั้งค่าปกติ (ถือไอเทมรีโมทคลิกขวา)
3. เลื่อนลงมาล่างสุด จะเจอส่วน **[ ส่วนตั้งค่าสำหรับแอดมิน ]** (ถ้าไม่มี Tag จะไม่เห็น)
4. กรอกข้อมูลใหม่ในช่องที่ต้องการ:
   - **ที่อยู่ไอพี (IP Address)**: ใส่ IP เครื่องที่รันบอท (เว้นว่าง = ใช้ค่าเดิม)
   - **หมายเลขพอร์ต (Port)**: ใส่พอร์ต (เว้นว่าง = ใช้ค่าเดิม)
5. **วิธีกู้คืนค่าเดิม:** เปิดปุ่ม "รีเซ็ตการตั้งค่า (Reset Default)" แล้วกด Submit
6. ระบบจะบันทึกและเริ่มทำงานใหม่ทันที!

---

## ❓ ปัญหาที่พบบ่อย (Troubleshooting)

- **Q: เพื่อนไม่ได้ยินเสียง?**
  - A: เช็คว่าชื่อในเกมกับ Discord ตรงกันไหม? และทั้งคู่เข้าห้องเสียงหรือยัง?
- **Q: ไม่เห็นปุ่มตั้งค่าแอดมิน?**
  - A: คุณต้องพิมพ์ `/tag @s add admin` ก่อนนะ
- **Q: บอทไม่ทำงาน?**
  - A: เช็คหน้าจอดำๆ (Terminal) ว่าบอทยังรันอยู่ไหม ห้ามปิดจอนั้นนะ!

---
**สร้างสรรค์โดย:** ZirconX
*ขอให้สนุกกับการคุยนะ! 🎉*
